package com.university.parking.model;

/**
 * Enumeration representing different types of vehicles that can park in the system.
 * Each vehicle type has specific parking restrictions and compatibility with spot types.
 */
public enum VehicleType {
    MOTORCYCLE,
    CAR,
    SUV_TRUCK,
    HANDICAPPED
}